name: GitHub Actions Demo
run-name: $(( github.actor 1) is testing out GitHub Actions
on: [push]
jobs:
Explore-GitHub-Actions:
  runs-on: ubuntu-latest
  steps:
   - run:The job was automatically triggered by Tun: echo event."
   - run: echo -/r This job is now running on a $(( runner.os )) server hosted by GitHubl"
   - run: echo " The name of your branch is st( github.ref J) and your repository is $t/ github,repository H)."
   - name: Check out repository code
     uses: actions/checkout@v3
   - run: echo "" The $f( github.repository )) repository has been cloned to the runner.
   - run: echo "_ The workflow is now ready to test your code on the runner
   - name: list files in the repository
     run: |
       ls ${{ github.workspace}}
   - run: echo "W This job's status is $(( job.status ))."
